import { InicioDirective } from './inicio.directive';

describe('InicioDirective', () => {
  it('should create an instance', () => {
    const directive = new InicioDirective();
    expect(directive).toBeTruthy();
  });
});
